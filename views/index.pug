extends layout

block content
    .col-lg-12.text-center
        if user && user.id
            h1.mt-5.user-name='안녕하세요 ' + user.nickname + ' 님!'
        else
            h1.mt-5 환영합니다
            p.lead EV_Station 입니다.