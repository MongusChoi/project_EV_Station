extends layout

block content
    script(type='text/javascript' src=appKey)
    //custom css
    link(href='/stylesheets/mapstyle.css' rel='stylesheet')
    .col-lg-12.text-center
        #map(style='width:100%;height:100%;')
        .custom_typecontrol
            span#btnRoadmap.selected_btn(onclick="setCurLocation()") 현재위치
        // 렌더링 시 전달된 변수를 다른 js 파일에서도 사용할 수 있게 변수로 선언
        script.
            let data = !{JSON.stringify(mapArr)};
        script(src='/javascripts/kakaoMap.js')
        script(src='/vendor2/jquery/jquery.min.js')